# URL Shortener CLI

URL Shortener — консольное приложение для сокращения ссылок с ограничением по количеству переходов и времени жизни.  
Поддерживает мультипользовательский режим с идентификацией по UUID.

---

## 📌 Основные возможности

- Создание коротких ссылок для длинных URL.
- Уникальные ссылки для каждого пользователя.
- Ограничение по количеству переходов (click limit).
- Время жизни ссылки (TTL), после которого ссылка автоматически удаляется.
- Автоматическое уведомление о недоступности ссылки.
- Мультипользовательский режим (UUID).
- Управление ссылками через CLI: просмотр, удаление, переход по короткой ссылке.

---

## 🏗️ Архитектура

```plain text
src/
├── main/java/com/apkq/
│   ├── Application.java                 # Точка входа
│   ├── cli/
│   │   ├── Cli.java                    # Основной CLI обработчик
│   │   ├── CliContext.java             # Контекст CLI сессии
│   │   ├── CommandRegistry.java        # Реестр команд
│   │   └── commands/
│   │       ├── CreateCommand.java
│   │       ├── DeleteCommand.java
│   │       ├── ExitCommand.java
│   │       ├── HelpCommand.java
│   │       ├── ListCommand.java
│   │       ├── OpenCommand.java
│   │       └── SwitchUserCommand.java
│   ├── core/
│   │   ├── models/
│   │   │   ├── User.java
│   │   │   └── UserLink.java
│   │   └── services/
│   │       ├── LinkService.java        # Сервис для работы со ссылками
│   │       └── UserManager.java        # Управление пользователями
│   ├── infrastructure/
│   │   └── config/
│   │       └── Configurator.java       # Конфигурация приложения
│   └── interfaces/
│       └── cli/
│           └── commands/
│               └── CliCommand.java     # Базовый интерфейс команд
```

---

## 🛠️ Команды

* LL - Long Link.
* SL - Short Link.
* CL - Click Limit.

Параметры можно указывать как в самой команде, так и после ее вызова.

| Команда            | Описание                                                                |
|--------------------|-------------------------------------------------------------------------|
| `su [UUID]`        | Переключение на пользователя по UUID (если не указан, создаётся новый). |
| `create [LL] [CL]` | Создать короткую ссылкую                                                |
| `open [SL]`        | Перейти по короткой ссылке.                                             |
| `list`             | Показать все короткие ссылки текущего пользователя.                     |
| `delete [SL]`      | Удалить короткую ссылку.                                                |
| `help`             | Показать список доступных команд.                                       |
| `exit`             | Выйти из приложения.                                                    |

---

## ⚡ Пример использования

### Создание ссылки

```bash
> create
Enter original link: https://google.com
Enter click limit (0 for unlimited): 2

✅ Created successfully!
Short link: http://localhost:8080/736e588
Original: https://google.com
Expires at: 11.11.2025 16:04
Click limit: 2
```

### Просмотр ссылок

```bash
> list
Your short URLs:
- http://localhost:8080/736e588 -> https://google.com | Clicks: 0/2 | Expires: 11.11.2025 16:04 | Status: ✅ Active
```

### Переключение пользователя

Создание нового пользователя (если UUID не указан):

```bash
> su
✅ New user created: 1a2b3c4d-5e6f-7g8h-9i0j-k1l2m3n4o5p6
```

### Удаление ссылки

```bash
> delete http://localhost:8080/736e588
✅ Short URL deleted: http://localhost:8080/736e588
```

---

## 🔧 Сборка и запуск

1. Клонируйте репозиторий проекта:

    ```bash
    git clone <репозиторий>
    cd <папка-проекта>
    ```

2. Создайте .env файл, скопировав пример .env.example в корне проекта:
   ```Bash
   cp .env.example .env
   ```
   
3. Скомпилируйте проект с помощью Maven:
   ```bash
   mvn clean compile
   ```

4. Запустите приложение:
   ```bash
   mvn exec:java -Dexec.mainClass="com.qpkq.Application"
   ```
